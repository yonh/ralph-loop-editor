{
  "name": "ralph-loop-editor-glm",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 1420 --strictPort",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0 --port 1421 --strictPort"
  },
  "devDependencies": {
    "vite": "^5.4.10"
  },
  "dependencies": {
    "@xterm/addon-fit": "^0.11.0",
    "@xterm/addon-web-links": "^0.12.0",
    "@xterm/xterm": "^6.0.0"
  }
}
